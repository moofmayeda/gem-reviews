<div class="page-header">
  <h1><%= @user.name %> <small><%= @user.email %> <span class='badge'><%= @user.reviews.count %> reviews</span></small></h1>
</div>

<ul>
  <% @user.reviews.each do |review| %>
    <li><%= link_to review.jewel.name, jewel_path(review.jewel) %></li>
  <% end %>
</ul>
